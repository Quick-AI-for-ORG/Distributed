syntax = "proto3";
package distributed;

import "Game.proto";
import "Result.proto";
import "GameServer.proto";

service Client {
    rpc recieveUpdate (Context) returns (Result);
    rpc changeServer (GameServer) returns (Result);
}